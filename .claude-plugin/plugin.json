{
  "name": "chrome-extension-builder",
  "displayName": "Chrome Extension Builder",
  "version": "1.0.0",
  "description": "Use when scaffolding, validating, testing, or publishing a Manifest V3 Chrome/Edge/Firefox extension. Builds end-to-end with WXT, Plasmo, or CRXJS — service-worker dispatcher, typed message passing, strict CSP, `_locales/` i18n, Playwright smoke harness, Chrome Web Store submission prep. Ships reference patterns from MetaMask, Bitwarden, Refined GitHub, Dark Reader, uBlock Origin Lite, Violentmonkey, SponsorBlock, and Stylus.",
  "author": {
    "name": "Chrome Extension Builder Contributors",
    "url": "https://github.com/harry-harish/chrome-extension-builder"
  },
  "homepage": "https://github.com/harry-harish/chrome-extension-builder",
  "repository": "https://github.com/harry-harish/chrome-extension-builder",
  "license": "Apache-2.0",
  "keywords": [
    "chrome-extension",
    "browser-extension",
    "manifest-v3",
    "mv3",
    "wxt",
    "plasmo",
    "crxjs",
    "firefox-extension",
    "edge-extension",
    "web-store",
    "scaffolding",
    "claude-code-plugin"
  ],
  "userConfig": {
    "default_framework": {
      "type": "string",
      "title": "Default framework",
      "description": "Default framework to scaffold with. WXT is recommended for 2026 (active maintenance, MV3-only, multi-browser, smallest bundle).",
      "enum": ["wxt", "plasmo", "crxjs", "vanilla"],
      "default": "wxt"
    },
    "target_browsers": {
      "type": "string",
      "title": "Target browsers",
      "description": "Comma-separated list of target browsers (chrome, edge, firefox, safari). WXT supports all four; Plasmo and CRXJS are Chromium-first.",
      "default": "chrome,edge,firefox"
    },
    "default_ui_framework": {
      "type": "string",
      "title": "UI framework",
      "description": "Frontend framework for popup/options/side-panel UIs.",
      "enum": ["react", "vue", "svelte", "solid", "vanilla"],
      "default": "react"
    },
    "typescript": {
      "type": "boolean",
      "title": "Use TypeScript",
      "description": "Scaffold with TypeScript (strongly recommended; every reference extension in 2026 uses TS).",
      "default": true
    },
    "package_manager": {
      "type": "string",
      "title": "Package manager",
      "description": "Package manager for installing dependencies.",
      "enum": ["pnpm", "npm", "yarn", "bun"],
      "default": "pnpm"
    }
  }
}
